# Quality Lens Configuration for shadcn/ui
# This config is inherited by all workspace packages
# Uses pnpm exec to resolve tools in pnpm workspace and produce JSON output

lenses:
  # Linting - eslint with JSON format
  eslint: pnpm exec eslint . --format json

  # Type checking - tsc with listFiles for complete file list
  typescript: pnpm exec tsc --noEmit --listFiles

  # Formatting - prettier check with debug for file list
  prettier: pnpm exec prettier --check "**/*.{ts,tsx,mdx}" --cache --log-level debug

  # Testing - vitest with JSON reporter and coverage
  vitest: pnpm exec vitest run --reporter=json --coverage

# Exclude test fixtures, deprecated packages, and templates
exclude:
  - "packages/tests/fixtures/**"
  - "packages/shadcn/test/fixtures/**"
  - "deprecated/**"
  - "templates/**"
  - "**/node_modules/**"
  - "**/temp/**"
